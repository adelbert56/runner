# 本地收尾整理

更新時間：2026-05-17

這份筆記記錄本機 `D:\Users\Squall\Documents\Runner` 的收尾狀態，避免後續代理人把本地驗證產物、救援資料或依賴資料夾誤判成專案變更。

## 目前狀態

- Git 分支：`main`
- 遠端狀態：`main...origin/main`
- 最新收尾提交：`f99c9aa docs(ops): Clarify automation closeout`
- 公開網站：[https://adelbert56.github.io/runner/](https://adelbert56.github.io/runner/)
- 專案定位：可收尾，進入排程觀察與例行維護。

## 本地資料夾判斷

| 路徑 | 狀態 | 處理原則 |
| --- | --- | --- |
| `.venv/` | 本機 Python 環境 | 保留；除非要重建環境，不要清。 |
| `node_modules/` | 本機 Node 依賴 | 保留；缺依賴時再重裝。 |
| `output/playwright/` | UI layout 與真瀏覽器驗證截圖 | 保留作為收尾驗證證據；可在下一輪 UI 驗證後覆蓋。 |
| `.codex-rescues/` | Codex 對話修復與 log archive | 保留；這是對話框崩潰修復證據，不提交。 |
| `scripts/**/__pycache__/` | Python cache | 可清理；已於 2026-05-17 清除。 |
| `runner/.obsidian/workspace.json` | Obsidian 本機 UI 狀態 | 不提交。 |

以上本地產物都已在 `.gitignore` 中忽略。

## 收尾驗證紀錄

- `npm run check`：通過。
- `npm run data:quality:strict`：通過。
- `npm run content:quality:strict`：通過。
- GitHub CI：PR #3 通過後已合併。
- 公開站：回 `200 OK`，頁面包含 `跑者廣場`、`app.js`、`styles.css`。

## 排程觀察

專案接下來的重點不是再補功能，而是確認 GitHub Actions 排程會穩定自動化：

- `Refresh race weather`：每天 07:00 Asia/Taipei，更新 7 天內賽事天氣。
- `Collect content candidates`：每週一 09:00 Asia/Taipei，更新跑鞋與新聞候選內容。
- `Refresh race data`：每週二、四 18:00 Asia/Taipei，重跑賽事爬蟲、官方補資料與品質報告。
- `Deploy static site`：推送 `main` 後部署 GitHub Pages。
- `CI`：推送、PR、手動執行時驗證品質 gate 與 UI layout。

若排程成功但沒有新增 commit，不一定是失敗；可能只是資料沒有變動。以 Actions run 結論與報告內容為準。

## 後續非阻塞事項

- 補 `LICENSE`，授權需由專案 owner 決定。
- Hero 圖目前使用 Unsplash 外部圖片；公開站可用，但若要降低外部依賴，後續可改成本地圖片。
- 原始資料完整度與官方直連率目前低於長期目標，屬於維護 backlog，不阻擋收尾。
- `運動筆記` 平台仍是主要補強來源；優先等排程重查，再決定是否修 parser。
