# UI-001 驗證證據

## 實作範圍

- `site/trainer-render.js`：新增 `renderCoachDecisionFlowCard(runs)`，在有實跑與尚無實跑兩種分析狀態都先呈現判讀順序。
- `site/trainer.css`：新增 `coach-flow-*` 樣式；桌機四欄、900px 以下兩欄、720px 以下將狀態標籤改為換行顯示。
- `site/trainer.html`：更新 renderer cache-bust 版本。

## 驗證結果

| 檢查 | 結果 |
|---|---|
| `node --check site/trainer-render.js` | 通過 |
| `npm run ui:check` | 通過；包含流程帶的責任邊界斷言 |
| `npm run trainer:logic` | 通過 |
| `UI_LAYOUT_PORT=4175 UI_LAYOUT_TRAINER_ONLY=1 UI_LAYOUT_VIEWPORTS=mobile,desktop npm run ui:layout` | 通過 |
| `git diff --check` | 通過 |

## 畫面證據

- `output/playwright/desktop-trainer-report.png`
- `output/playwright/mobile-trainer-report.png`

## 邊界

- 元件只解釋既有判讀責任，不觸發 Garmin 同步、課表寫入或外部 API 動作。
- 既有 4174 埠被其他服務占用；改用隔離的 4175 埠進行本次 layout 驗證。
